services:
  app:
    build: .
    container_name: pulse-xray
    ports:
      - "5000:5000"
    volumes:
      - data:/app/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  data: